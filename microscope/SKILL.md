---
name: microscope
description: >
  Use when 要给"有内部复杂度的组件/模块"编写让产品和工程师都能看懂的技术设计文档/设计说明——
  把"输入→输出主流程 + 内部为各种 case 做的适配"讲清楚。触发短语:"写技术文档"、"写设计文档"、
  "给这个组件写设计说明"、"讲清这个组件内部怎么实现的"、"用 microscope"、
  "write a design doc for this component"、"explain how this component works internally"。
  Do NOT use for: API 参考 / README / 用户手册 / changelog / commit message(那些不是内部设计文档)。
---

> 本 skill 受 `references/constitution.md` 约束(always-follow,跨 skill 通用价值观/安全/身份层)

# microscope —— 把复杂组件讲到非技术读者也能看懂

## Overview

**核心心智:先在显微镜下看清,再翻译给外行。** 给一个有内部复杂度的组件写设计文档,最容易写成
"贴 API / 贴函数名 / 罗列实现",结果只有原作者看得懂。microscope 强制两件事:
**① 按"功能效果(用户视角)→ 核心改动(技术)→ 依赖顺序(独立效果的判断先后)"分层组织;
② 写完用一个"只懂领域、不懂技术栈"的 persona 子 agent 复审,卡壳就改,直到外行能顺下来。**

## When to Use

- 给前端组件 / 后端模块 / 任何"输入→输出之间藏了一堆 case 处理"的单元写设计文档。
- 别人(PM / 新人 / 跨栈同事)要看懂"这东西是干嘛的 + 内部为什么这么实现"。

**不适用**:API 参考、README、用户手册、changelog、commit message——那些不需要"讲内部因果"。组件很简单 / 纯展示(如一个 Button)、内部没有 case 处理时也不必用——没有"内部怎么实现"可讲。

## Required Workflow

按顺序执行,**第 5 步(persona 复审)不是可选项**:

1. **先吃透源码**。读出"输入→输出主流程",以及内部**为每个 case 做了什么适配、按什么顺序**。读不透不动笔——猜着写出来的因果会误导读者。
2. **按固定结构写**(见《文档结构》)。
3. **分层表达**。功能效果用大白话/用户视角;§1/§3/核心改动给工程师,但**每条都带"为什么 / 否则会怎样"的因果**,不只罗列"做了什么"。
4. **画效果图**。有合适绘图工具就用工具;布局/层叠/冻结这类像素效果用 **ASCII 等宽代码块**(mermaid 画不了表格布局,ASCII 在 markdown/Lark 代码块里能保等宽对齐)。
5. **persona 可懂性复审闭环**。派一个 subagent 扮演"**只懂该领域(如表格/电子表格)、完全不懂这个技术栈(如 antd/React/CSS)**"的读者,**只读文档、不读源码**,逐节标出看不懂的术语 / 绕的表述 / ASCII 图歧义;按反馈改,必要时再审一轮,直到该读者能从头顺到尾。

## 文档结构

| 段 | 写什么 | 给谁看 |
|---|---|---|
| **术语表** | 领域读者会反复遇到的词;**同名不同义必须消歧**(例:"锁排序" vs "锁列") | 所有人 |
| **§1 总览** | 为处理这些 case 写了哪些逻辑、**先后顺序**(前一步是后一步的输入) | 工程师 |
| **§2 各 case** | 每个 case = **功能效果**(用户视角,看得见摸得着,配 ASCII 图)+ **核心改动**(技术实现,带因果) | 功能效果给所有人;核心改动给工程师 |
| **§3 依赖顺序** | **互相独立的效果叠在同一处时,谁必须先判断 / 谁压谁,否则冲突** | 工程师 |

## 关键约束(最容易写歪的四处)

- **§3"依赖顺序"≠ 数据流顺序**。不是"先算 A 才能算 B"那种自然先后(那归 §1),而是**两个本来不相关的效果落在同一格 / 同一像素 / 同一次点击上,必须按特定顺序或优先级判断谁压谁,否则视觉穿帮或交互打架**(典型:z-index 层叠、点击事件抢占、样式优先级)。每条都写清"判错会出什么问题"。
- **功能效果必须是用户视角**,不是技术动作。写"拖动列右边线,这列实时变宽、其他列不动",不是"mousedown 记 offsetWidth 再 clamp"。
- **核心改动必须带因果**,不能只列"做了 X、Y、Z";每条接一句"为了…… / 否则会……"。
- **persona 复审必跑**。自己觉得清楚 ≠ 外行看得懂;不跑复审等于没做完。

## 一个最小范例(节选,展示分层)

```markdown
### Case A:拖一列改宽

**功能效果**:鼠标移到两列之间的竖线上,光标变成左右拖拽样式;按住拖动,这一列宽度
实时跟手,**其他列不动、表格整体宽度随之变化**;松手定宽。

  拖前 │Name│Age│City│   ──►   拖后 │Name      │Age│City│   (只有 Name 变宽)

**核心改动**:① 列宽存成组件自己的状态,拖动就改它;② 用样式把表格总宽钉死、关掉浏览器
自动分配列宽——**否则拖一列会按比例牵动其他列**;③ 起算宽用"连内边距的真实宽",否则手柄
会越拖越落在鼠标后面。
```

(功能效果零术语 + ASCII 图;核心改动逐条带"否则会……"。)

## Common Mistakes

| 写歪的样子 | 改成 |
|---|---|
| 功能效果里贴 `mousedown` / `useMemo` / 函数名 | 功能效果只讲"用起来什么样",技术词挪进核心改动 |
| §3 写成 collectLeaf → leafStates 的数据流 | §3 只写"独立效果的判断先后";数据流归 §1 |
| 核心改动罗列"做了什么"没因果 | 每条补一句"否则会怎样" |
| 写完直接交付,没派 persona 复审 | 必跑复审,改到外行能顺下来 |
| 同一个词指两件事(如"锁") | 术语表消歧,正文统一用词 |

## Red Flags — 停下来重写

- 功能效果段里出现技术术语 / 函数名 → 该挪到核心改动。
- §3 里写的是"先算谁后算谁"的自然顺序 → 那是 §1 的料,§3 要的是"谁压谁、否则穿帮"。
- 想跳过 persona 复审("我觉得挺清楚的") → 不跑就是没做完。
